import React from 'react';

const PageTwo = () => {
  return (
    <div>
      <h1>Page Two</h1>
      <p>This is the second additional page.</p>
      {/* API Call Example */}
      {/* Example: fetch('/api/page-two').then(response => response.json()).then(data => setData(data)); */}
    </div>
  );
};

export default PageTwo;
