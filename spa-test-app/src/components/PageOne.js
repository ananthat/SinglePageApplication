import React from 'react';

const PageOne = () => {
  return (
    <div>
      <h1>Page One</h1>
      <p>This is the first additional page.</p>
      {/* API Call Example */}
      {/* Example: fetch('/api/page-one').then(response => response.json()).then(data => setData(data)); */}
    </div>
  );
};

export default PageOne;
