import React from 'react';

const HomePage = () => {
  return (
    <div>
      <h1>Welcome to the Home Page</h1>
      <p>This is the home page of our SPA test application.</p>
      {/* API Call Example */}
      {/* Example: fetch('/api/home').then(response => response.json()).then(data => setData(data)); */}
    </div>
  );
};

export default HomePage;
